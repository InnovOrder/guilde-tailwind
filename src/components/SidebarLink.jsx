import React from 'react'

const SidebarLink = ({title}) => {
    return (
        <div>
            <h4 className="bg-blue-100 text-left text-blue-700 p-2 cursor-help">{title}</h4>
        </div>
    )
}

export default SidebarLink
